<h1>メッセージ一覧</h1>

<ul>
  <% @messages.each do |m| %>
    <li><%= m.id %> : <%= link_to m.title, m %> : <%= m.content %> : <%= m.created_at %></li>
  <% end %>
</ul>

<%= link_to '新規メッセージの投稿', new_message_path %>